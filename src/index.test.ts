
describe("Sample tests", () => {
    test("Example test 1", () => {
        expect(1 + 1).toBe(2);
    })
    test("Example test 2", () => {
        expect({ hello: "world" }).toMatchObject({ hello: "world" })
    })
})

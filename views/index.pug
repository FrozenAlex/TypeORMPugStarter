extends layout.pug

block content
    h1.centered-text Hello Pog
    if title
        //- Escaped string
        h2 #{title}
    - var a = [1,2,3,4,5,6]
    ul
        each val in a
            li= val
